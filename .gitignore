# colcon build artifacts
build/
install/
log/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Vendored upstream sources — these have their own .git and are
# cloned/updated from upstream, not tracked here.
src/rtabmap/
src/rtabmap_ros/

# SLAM maps produced by scripts/sim_launcher_gui.py. These are per-machine
# recording artifacts (a .pgm can be megabytes), not source — the worlds that
# generate them ARE tracked, so any map can be re-made.
maps/

# Editor / OS
*.swp
.DS_Store
